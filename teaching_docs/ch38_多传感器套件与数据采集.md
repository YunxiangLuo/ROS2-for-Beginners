# 第38章 多传感器套件与数据采集

## 学习目标
- 掌握CARLA中RGB/Depth/Semantic Segmentation相机配置
- 理解LiDAR传感器参数与点云数据处理
- 学会RADAR、GNSS、IMU传感器的使用
- 理解传感器标定的内参/外参概念
- 掌握Rosbag数据采集与回放方法

## 38.1 相机传感器

### 38.1.1 相机类型

CARLA提供了三种相机传感器，分别适用于不同任务：

| 相机类型 | 输出 | 编码 | 用途 |
|---------|------|------|------|
| RGB相机 | 彩色图像 | BGRA 8-bit | 视觉感知、目标检测 |
| Depth相机 | 深度图 | 32位浮点 | 距离估计、3D重建 |
| Semantic Segmentation相机 | 语义标签图 | 32位标签 | 语义理解 |

```python
# 三种相机的配置示例
rgb_camera_bp = blueprint_library.find('sensor.camera.rgb')
depth_camera_bp = blueprint_library.find('sensor.camera.depth')
semseg_camera_bp = blueprint_library.find('sensor.camera.semantic_segmentation')
```

### 38.1.2 相机参数配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| image_size_x | 1920 | 图像宽度(像素) |
| image_size_y | 1080 | 图像高度(像素) |
| fov | 90.0 | 视场角(度) |
| sensor_tick | 0.0 | 传感器采样间隔(秒) |
| lens_flare | False | 是否启用镜头光晕 |
| gamma | 2.2 | 伽马校正值 |
| motion_blur_intensity | 0.0 | 运动模糊强度 |
| motion_blur_max_distortion | 0.0 | 运动模糊最大失真 |

```python
# 完整相机配置
camera_bp.set_attribute('image_size_x', '800')
camera_bp.set_attribute('image_size_y', '600')
camera_bp.set_attribute('fov', '90')
camera_bp.set_attribute('sensor_tick', '0.05')  # 20 FPS
camera_transform = carla.Transform(
    carla.Location(x=1.6, z=1.7),
    carla.Rotation(pitch=0, yaw=0, roll=0)
)
rgb_camera = world.spawn_actor(camera_bp, camera_transform, attach_to=vehicle)
```

### 38.1.3 内参矩阵与畸变模型

相机内参矩阵K将3D世界坐标投影到2D像素平面：

```
K = [fx   0   cx]
    [0   fy   cy]
    [0    0    1]
```

- fx, fy: 焦距(像素单位), `fx = F * image_size_x / sensor_width`
- cx, cy: 主点坐标, 通常为图像中心

```python
# 从CARLA相机中获取内参
def get_camera_intrinsics(camera_actor, image):
    h, w = image.height, image.width
    fov = camera_actor.attributes['fov']
    fov = float(fov)

    fx = w / (2.0 * math.tan(fov * math.pi / 360.0))
    fy = h / (2.0 * math.tan(fov * math.pi / 360.0))
    cx = w / 2.0
    cy = h / 2.0

    K = np.array([[fx, 0, cx],
                  [0, fy, cy],
                  [0,  0,  1]])
    return K
```

CARLA相机默认使用 **针孔模型(pinhole model)**，无畸变(畸变系数为0)。如需模拟真实相机畸变，可在后处理中添加：

```python
# 模拟径向畸变
def apply_distortion(image_np, k1=0.05, k2=0.02):
    h, w = image_np.shape[:2]
    K = np.array([[w/2, 0, w/2],
                  [0, h/2, h/2],
                  [0, 0, 1]])

    mapx, mapy = cv2.initUndistortRectifyMap(
        K, np.array([k1, k2, 0, 0, 0]),
        None, K, (w, h), cv2.CV_32FC1
    )
    return cv2.remap(image_np, mapx, mapy, cv2.INTER_LINEAR)
```

### 38.1.4 Depth与Semantic Segmentation数据处理

Depth相机输出需要进行解码才能得到实际距离：

```python
# Depth图像解码（CARLA编码方式）
def decode_depth_image(raw_data):
    import numpy as np
    # raw_data: 4-ch BGRA, depth编码在R/G/B通道
    data = np.frombuffer(raw_data, dtype=np.uint8)
    data = data.reshape((raw_data.height, raw_data.width, 4))
    data = data[:, :, :3]  # 取BGR通道

    # 解码: (R + G*256 + B*256*256) / (256*256*256 - 1) * 1000
    depth = data[:, :, 2] * 256 * 256 + data[:, :, 1] * 256 + data[:, :, 0]
    depth = depth / (256 * 256 * 256 - 1)
    depth = depth * 1000.0  # 转换为米
    return depth

# Semantic Segmentation标签映射
# 0: 未标注, 1: 建筑, 2: 围栏, 3: 其他, 4: 行人, 5: 路杆
# 6: 道路, 7: 人行道, 8: 植被, 9: 车辆, 10: 墙, 11: 交通标志
```

## 38.2 激光雷达传感器

### 38.2.1 LiDAR配置参数

CARLA支持高精度模拟的LiDAR传感器，主要参数如下：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| channels | 32 | 激光通道数(线束) |
| range | 100.0 | 最大探测距离(米) |
| points_per_second | 560000 | 每秒点云数量 |
| rotation_frequency | 10.0 | 旋转频率(Hz) |
| upper_fov | 10.0 | 垂直FOV上限(度) |
| lower_fov | -30.0 | 垂直FOV下限(度) |
| horizontal_fov | 360.0 | 水平FOV(度) |
| sensor_tick | 0.0 | 采样间隔 |
| dropoff_general_rate | 0.0 | 通用丢点率 |
| dropoff_intensity_limit | 0.0 | 强度丢点阈值 |
| dropoff_zero_intensity | 0.0 | 零强度丢点率 |

```python
# 配置64线激光雷达
def setup_lidar(world, blueprint_library, vehicle):
    lidar_bp = blueprint_library.find('sensor.lidar.ray_cast')
    lidar_bp.set_attribute('channels', '64')
    lidar_bp.set_attribute('range', '100')
    lidar_bp.set_attribute('points_per_second', '1000000')
    lidar_bp.set_attribute('rotation_frequency', '20')
    lidar_bp.set_attribute('upper_fov', '15')
    lidar_bp.set_attribute('lower_fov', '-25')
    lidar_bp.set_attribute('sensor_tick', '0.05')  # 20Hz
    lidar_bp.set_attribute('dropoff_general_rate', '0.1')  # 10%噪声

    lidar_transform = carla.Transform(
        carla.Location(x=0.0, z=2.0),
        carla.Rotation(pitch=0, yaw=0, roll=0)
    )
    lidar = world.spawn_actor(lidar_bp, lidar_transform, attach_to=vehicle)
    return lidar
```

### 38.2.2 点云格式与处理

LiDAR传感器输出为原始点云数据，需转换为标准格式：

```python
import numpy as np

def lidar_raw_to_pointcloud(raw_data):
    """
    将CARLA LiDAR原始数据转换为Nx4的numpy数组
    每行: [x, y, z, intensity]
    """
    points = np.frombuffer(raw_data.raw_data, dtype=np.float32)
    points = points.reshape(-1, 4)

    # 坐标系转换: CARLA (x前, y右, z上) -> ROS (x前, y左, z上)
    # CARLA的Y轴与ROS相反
    points[:, 1] = -points[:, 1]

    return points

def pointcloud_to_ros2_msg(points, timestamp, frame_id='lidar_link'):
    """numpy点云 → sensor_msgs/PointCloud2"""
    from sensor_msgs.msg import PointCloud2, PointField

    msg = PointCloud2()
    msg.header.stamp = timestamp
    msg.header.frame_id = frame_id
    msg.height = 1
    msg.width = points.shape[0]
    msg.fields = [
        PointField(name='x', offset=0, datatype=PointField.FLOAT32, count=1),
        PointField(name='y', offset=4, datatype=PointField.FLOAT32, count=1),
        PointField(name='z', offset=8, datatype=PointField.FLOAT32, count=1),
        PointField(name='intensity', offset=12, datatype=PointField.FLOAT32, count=1),
    ]
    msg.is_bigendian = False
    msg.point_step = 16
    msg.row_step = msg.point_step * msg.width
    msg.is_dense = True
    msg.data = points.tobytes()
    return msg
```

### 38.2.3 噪声模型配置

通过设置丢点率参数可以模拟真实LiDAR的噪声特性：

```bash
# 不同LiDAR型号的典型参数
# Velodyne VLP-16: 16线, 100m, 300000点/秒, ±15°垂直FOV
# Ouster OS1-64: 64线, 120m, 1310720点/秒, ±22.5°垂直FOV
# Hesai Pandar40P: 40线, 200m, 720000点/秒, ±15°垂直FOV
```

## 38.3 毫米波雷达与GNSS/IMU

### 38.3.1 RADAR传感器

RADAR传感器输出目标级检测结果，包含距离、速度、方位角等信息：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| horizontal_fov | 30.0 | 水平视场角(度) |
| vertical_fov | 5.0 | 垂直视场角(度) |
| range | 100.0 | 最大探测距离(米) |
| points_per_second | 1500 | 每秒检测点数 |
| sensor_tick | 0.0 | 采样间隔 |

```python
def setup_radar(world, blueprint_library, vehicle):
    radar_bp = blueprint_library.find('sensor.other.radar')
    radar_bp.set_attribute('horizontal_fov', '60')
    radar_bp.set_attribute('vertical_fov', '10')
    radar_bp.set_attribute('range', '50')
    radar_bp.set_attribute('points_per_second', '2000')
    radar_bp.set_attribute('sensor_tick', '0.05')  # 20Hz

    radar_transform = carla.Transform(
        carla.Location(x=2.0, z=0.5),
        carla.Rotation(pitch=0, yaw=0, roll=0)
    )
    radar = world.spawn_actor(radar_bp, radar_transform, attach_to=vehicle)
    return radar

# RADAR回调数据处理
def radar_callback(data):
    """
    RADAR检测点包含:
    - velocity: 径向速度(m/s)
    - azimuth: 水平方位角(rad)
    - altitude: 垂直仰角(rad)
    - depth: 距离(m)
    """
    for detection in data:
        print(f"距离: {detection.depth:.2f}m, "
              f"速度: {detection.velocity:.2f}m/s, "
              f"方位角: {detection.azimuth:.2f}rad")
```

### 38.3.2 GNSS传感器

GNSS传感器提供全局定位信息：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| noise_alt_bias | 0.0 | 海拔噪声偏置 |
| noise_alt_stddev | 0.0 | 海拔噪声标准差 |
| noise_lat_bias | 0.0 | 纬度噪声偏置 |
| noise_lat_stddev | 0.0 | 纬度噪声标准差 |
| noise_lon_bias | 0.0 | 经度噪声偏置 |
| noise_lon_stddev | 0.0 | 经度噪声标准差 |
| sensor_tick | 0.0 | 采样间隔 |

```python
def setup_gnss(world, blueprint_library, vehicle):
    gnss_bp = blueprint_library.find('sensor.other.gnss')
    gnss_bp.set_attribute('noise_alt_stddev', '0.5')
    gnss_bp.set_attribute('noise_lat_stddev', '0.0001')
    gnss_bp.set_attribute('noise_lon_stddev', '0.0001')
    gnss_bp.set_attribute('sensor_tick', '0.1')  # 10Hz

    gnss_transform = carla.Transform(carla.Location(x=0.0, z=0.0))
    gnss = world.spawn_actor(gnss_bp, gnss_transform, attach_to=vehicle)
    return gnss

# GNSS → ROS2 NavSatFix
from sensor_msgs.msg import NavSatFix

def gnss_to_navsatfix(gnss_data, timestamp):
    msg = NavSatFix()
    msg.header.stamp = timestamp
    msg.header.frame_id = 'gnss_link'
    msg.latitude = gnss_data.latitude
    msg.longitude = gnss_data.longitude
    msg.altitude = gnss_data.altitude
    msg.status.status = 0  # STATUS_FIX
    msg.status.service = 1  # SERVICE_GPS
    return msg
```

### 38.3.3 IMU传感器

IMU提供六轴数据：三轴加速度 + 三轴角速度：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| noise_accel_stddev_x | 0.0 | X轴加速度噪声标准差 |
| noise_accel_stddev_y | 0.0 | Y轴加速度噪声标准差 |
| noise_accel_stddev_z | 0.0 | Z轴加速度噪声标准差 |
| noise_gyro_stddev_x | 0.0 | X轴陀螺仪噪声标准差 |
| noise_gyro_stddev_y | 0.0 | Y轴陀螺仪噪声标准差 |
| noise_gyro_stddev_z | 0.0 | Z轴陀螺仪噪声标准差 |
| sensor_tick | 0.0 | 采样间隔 |

```python
def setup_imu(world, blueprint_library, vehicle):
    imu_bp = blueprint_library.find('sensor.other.imu')
    imu_bp.set_attribute('noise_accel_stddev_x', '0.1')
    imu_bp.set_attribute('noise_accel_stddev_y', '0.1')
    imu_bp.set_attribute('noise_accel_stddev_z', '0.1')
    imu_bp.set_attribute('noise_gyro_stddev_x', '0.05')
    imu_bp.set_attribute('noise_gyro_stddev_y', '0.05')
    imu_bp.set_attribute('noise_gyro_stddev_z', '0.05')
    imu_bp.set_attribute('sensor_tick', '0.02')  # 50Hz

    imu_transform = carla.Transform(carla.Location(x=0.0, z=0.0))
    imu = world.spawn_actor(imu_bp, imu_transform, attach_to=vehicle)
    return imu

# IMU → ROS2 Imu
from sensor_msgs.msg import Imu

def imu_to_ros2_msg(imu_data, timestamp):
    msg = Imu()
    msg.header.stamp = timestamp
    msg.header.frame_id = 'imu_link'

    # 加速度 (m/s^2)
    msg.linear_acceleration.x = imu_data.accelerometer.x
    msg.linear_acceleration.y = -imu_data.accelerometer.y
    msg.linear_acceleration.z = imu_data.accelerometer.z

    # 角速度 (rad/s)
    msg.angular_velocity.x = imu_data.gyroscope.x
    msg.angular_velocity.y = -imu_data.gyroscope.y
    msg.angular_velocity.z = imu_data.gyroscope.z

    # 朝向 (由加速度计和陀螺仪融合)
    msg.orientation.w = imu_data.compass
    return msg
```

## 38.4 传感器标定

### 38.4.1 内参标定

内参描述传感器内部参数。对于相机，内参为K矩阵；对于LiDAR，内参为通道角度分布。

```yaml
# camera_intrinsics.yaml
camera:
  image_width: 800
  image_height: 600
  camera_name: front_camera
  camera_matrix:
    rows: 3
    cols: 3
    data: [530.0, 0.0, 400.0, 0.0, 530.0, 300.0, 0.0, 0.0, 1.0]
  distortion_model: plumb_bob
  distortion_coefficients:
    rows: 1
    cols: 5
    data: [0.05, -0.02, 0.001, 0.002, 0.0]
```

### 38.4.2 外参标定

外参描述传感器之间的相对空间变换(TF变换)：

```yaml
# sensor_transforms.yaml
transforms:
  - frame_id: lidar_link
    child_frame_id: camera_front_link
    translation: [0.5, 0.0, 0.2]  # x, y, z (meters)
    rotation: [0.0, 0.0, 0.0, 1.0]  # quaternion (x, y, z, w)

  - frame_id: base_link
    child_frame_id: lidar_link
    translation: [1.5, 0.0, 2.0]
    rotation: [0.0, 0.0, 0.0, 1.0]

  - frame_id: base_link
    child_frame_id: gnss_link
    translation: [0.0, 0.0, 0.5]
    rotation: [0.0, 0.0, 0.0, 1.0]
```

### 38.4.3 sensor_tick与同步

`sensor_tick`控制传感器数据发布的频率。值为0表示每个仿真步长都发布：

| sensor_tick | 行为 |
|-------------|------|
| 0.0 | 每个仿真步长触发回调 |
| 0.05 | 20Hz固定频率 |
| 0.1 | 10Hz固定频率 |
| 0.033 | ~30Hz固定频率 |

```python
# 多传感器时间同步
# CARLA中通过world.on_tick()获取全局时间戳
def setup_synchronized_sensors(world, sensors, callback):
    def tick_callback(world_snapshot):
        timestamp = world_snapshot.timestamp
        for sensor in sensors:
            sensor.listen(lambda data: callback(data, timestamp))

    world.on_tick(tick_callback)
```

### 38.4.4 坐标系对齐

CARLA与ROS坐标系约定对比：

| 方向 | CARLA | ROS |
|------|-------|-----|
| X | 前 | 前 |
| Y | 右 | 左 |
| Z | 上 | 上 |

```python
# CARLA → ROS 坐标转换
def carla_to_ros_transform(carla_transform):
    """将CARLA Transform转换为ROS TF2 Transform"""
    location = carla_transform.location
    rotation = carla_transform.rotation

    # ROS中Y轴取反
    ros_x = location.x
    ros_y = -location.y
    ros_z = location.z

    # 欧拉角 → 四元数 (ROS中Yaw取反)
    import math
    roll = math.radians(rotation.roll)
    pitch = math.radians(rotation.pitch)
    yaw = math.radians(-rotation.yaw)

    cy = math.cos(yaw * 0.5)
    sy = math.sin(yaw * 0.5)
    cp = math.cos(pitch * 0.5)
    sp = math.sin(pitch * 0.5)
    cr = math.cos(roll * 0.5)
    sr = math.sin(roll * 0.5)

    qw = cy * cp * cr + sy * sp * sr
    qx = cy * cp * sr - sy * sp * cr
    qy = sy * cp * sr + cy * sp * cr
    qz = sy * cp * cr - cy * sp * sr

    return ros_x, ros_y, ros_z, qx, qy, qz, qw
```

## 38.5 数据采集与Rosbag

### 38.5.1 rosbag record使用

```bash
# 录制所有话题
ros2 bag record -a -o carla_dataset

# 录制特定话题（推荐）
ros2 bag record \
  /camera/rgb/image_raw \
  /camera/depth/image_raw \
  /camera/semseg/image_raw \
  /lidar/points \
  /radar/detections \
  /gnss/data \
  /imu/data \
  -o carla_sensor_data

# 录制带压缩
ros2 bag record \
  /camera/rgb/image_raw/compressed \
  /lidar/points \
  -o compressed_bag

# 录制指定时长（10秒）
timeout 10 ros2 bag record -a -o short_recording
```

### 38.5.2 数据同步与时间戳对齐

多传感器数据同步的关键是使用统一的时间戳基准：

```python
def sync_callback(rgb_msg, depth_msg, lidar_msg, gnss_msg):
    """ApproximateTimeSynchronizer同步回调"""
    # 所有消息时间戳对齐
    stamp = rgb_msg.header.stamp

    # 保存到数据集
    dataset_entry = {
        'timestamp': stamp.sec + stamp.nanosec * 1e-9,
        'rgb': rgb_msg,
        'depth': depth_msg,
        'lidar': lidar_msg,
        'gnss': gnss_msg
    }
    return dataset_entry

# ROS2 ApproximateTime同步策略
from message_filters import ApproximateTimeSynchronizer, Subscriber

def setup_synchronizer(node, topics, queue_size=10, slop=0.05):
    subs = [Subscriber(node, msg_type, topic)
            for msg_type, topic in topics]
    sync = ApproximateTimeSynchronizer(
        subs, queue_size, slop, allow_headerless=False
    )
    sync.registerCallback(sync_callback)
    return sync
```

### 38.5.3 数据集管理

```python
import os
from ament_index_python.packages import get_package_share_directory

class CarlaDatasetRecorder:
    def __init__(self, output_dir='./carla_datasets'):
        self.output_dir = output_dir
        self.sequence_id = 0
        self.frame_id = 0

    def create_sequence(self):
        seq_dir = os.path.join(self.output_dir, f'seq_{self.sequence_id:04d}')
        os.makedirs(os.path.join(seq_dir, 'rgb'), exist_ok=True)
        os.makedirs(os.path.join(seq_dir, 'depth'), exist_ok=True)
        os.makedirs(os.path.join(seq_dir, 'lidar'), exist_ok=True)
        os.makedirs(os.path.join(seq_dir, 'calib'), exist_ok=True)
        self.sequence_id += 1
        return seq_dir

    def save_frame_data(self, seq_dir, data_dict):
        import cv2
        # 保存RGB图像
        rgb = data_dict['rgb']
        cv2.imwrite(
            f'{seq_dir}/rgb/{self.frame_id:06d}.png',
            cv2.cvtColor(rgb, cv2.COLOR_BGR2RGB)
        )

        # 保存LiDAR点云 (npy格式)
        lidar = data_dict['lidar']
        np.save(f'{seq_dir}/lidar/{self.frame_id:06d}.npy', lidar)

        self.frame_id += 1
```

### 38.5.4 rosbag回放

```bash
# 查看bag信息
ros2 bag info carla_sensor_data

# 回放bag
ros2 bag play carla_sensor_data

# 回放指定速度
ros2 bag play carla_sensor_data --rate 0.5  # 半速播放

# 循环回放
ros2 bag play carla_sensor_data --loop

# 指定起始时间
ros2 bag play carla_sensor_data --start-offset 5  # 从第5秒开始
```

## 传感器架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    CARLA 仿真服务器                       │
│  ┌─────────┐  ┌──────────┐  ┌────────┐  ┌───────────┐ │
│  │ 相机     │  │ LiDAR    │  │ RADAR  │  │ GNSS/IMU  │ │
│  │ RGB/Depth│  │ 点云     │  │ 目标    │  │ 位姿      │ │
│  │ SemSeg   │  │ 64通道   │  │ 列表    │  │ 六轴      │ │
│  └────┬─────┘  └────┬─────┘  └───┬────┘  └─────┬─────┘ │
│       │             │            │              │        │
│       ▼             ▼            ▼              ▼        │
│  ┌──────────────────────────────────────────────────┐    │
│  │            ROS2桥接 (carla_ros_bridge)            │    │
│  │  Image  PointCloud2  RadarDetection  NavSatFix   │    │
│  └──────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
       │             │            │              │
       ▼             ▼            ▼              ▼
┌────────────────────────────────────────────────────────┐
│              rosbag record / rosbag play                 │
│              数据采集 ↔ 离线回放                          │
└────────────────────────────────────────────────────────┘
```

## 本章小结

- CARLA提供RGB/Depth/Semantic Segmentation三种相机，通过sensor_tick控制采样频率
- LiDAR支持通道数、FOV、丢点率等参数配置，输出Nx4点云
- RADAR输出目标级检测，GNSS输出经纬度，IMU输出六轴加速度+角速度
- 传感器标定包含内参(传感器内部参数)和外参(传感器间空间变换)
- CARLA与ROS坐标系差异需转换(Y轴取反)，通过TF2进行坐标系管理
- rosbag record用于数据采集，ApproximateTimeSynchronizer实现多传感器时间戳对齐
